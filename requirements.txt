# CIVET (CIF Validation and Editing Tool) Application Requirements
# For development and building standalone executable

# GUI Framework - Core requirement for the application
PyQt6>=6.4.0

# HTTP library for downloading CIF dictionaries from URLs
requests>=2.28.0

# Build tools for creating standalone executable (development only)
pyinstaller>=5.13.0

# Note: All other dependencies are part of Python standard library:
# - os, sys, re, json, tempfile, urllib.parse
# - typing, dataclasses, enum, datetime, pathlib
