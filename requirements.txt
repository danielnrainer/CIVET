# CIVET runtime requirements
# Dependencies required to run CIVET from source.

# GUI framework - core runtime requirement
PyQt6>=6.4.0

# HTTP client for dictionary downloads and update checks
requests>=2.28.0

# Build, development, and test tooling is listed in requirements-dev.txt.

# Note: all other dependencies are part of the Python standard library:
# - os, sys, re, json, tempfile, urllib.parse
# - typing, dataclasses, enum, datetime, pathlib
