# CIVET runtime and build requirements
# Runtime dependencies for launching CIVET from source.
# Build tooling for packaging is included below for convenience.

# GUI framework - core runtime requirement
PyQt6>=6.4.0

# HTTP client for dictionary downloads and update checks
requests>=2.28.0

# Build tooling (used when creating standalone executables)
pyinstaller>=5.13.0

# Development and test tooling is listed in requirements-dev.txt.

# Note: all other dependencies are part of the Python standard library:
# - os, sys, re, json, tempfile, urllib.parse
# - typing, dataclasses, enum, datetime, pathlib
